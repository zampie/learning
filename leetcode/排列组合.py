from scipy.special import comb, perm
from itertools import combinations, permutations


perm(6,3)
comb(6,3)



# --------------------
nums = [1,2,3]

list(permutations(nums, 2))
len(list(combinations(list(range(10)), 5)))
